builder = "railpack"

[packs]
  enabled = ["rust"]

[build]
  command = "cargo build --release"

[build.env]
  DEBIAN_PACKAGES = "pkg-config libssl-dev ca-certificates"

[start]
  command = "./target/release/rust_bot"
